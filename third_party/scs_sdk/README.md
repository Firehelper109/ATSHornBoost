# SCS SDK dependency

Download SCS Telemetry & Input SDK 1.14 from SCS Software and copy its `include` directory here so this path exists:

```text
third_party/scs_sdk/include/scssdk.h
```

The Visual Studio project expects the SDK headers at `third_party\scs_sdk\include`.

The SDK itself is maintained and licensed by SCS Software.
